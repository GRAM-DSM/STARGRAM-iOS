import SwiftUI

struct SearchView: View {
    var body: some View {
        Text("검색 화면입니다.")
    }
}

struct SearchView_Previews: PreviewProvider {
    static var previews: some View {
        SearchView()
    }
}
